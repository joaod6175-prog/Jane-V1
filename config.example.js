/* ============================================
   JARVIS MOBILE AI — CONFIG EXAMPLE
   ============================================

   INSTRUÇÕES:
   1. Copie este arquivo e renomeie para: config.js
   2. Substitua os valores abaixo pelas suas chaves reais
   3. NUNCA faça commit de config.js no Git!
      Adicione config.js ao .gitignore

   Ou use o painel de Configurações (⚙) dentro do app
   — as chaves são salvas no localStorage do navegador.

   ============================================ */

// Chave da API do Google Gemini
// Obtenha em: https://aistudio.google.com/app/apikey
var GEMINI_API_KEY = 'SUA_CHAVE_GEMINI_AQUI';

// URL do projeto Supabase
// Encontrada em: Project Settings → API → Project URL
var SUPABASE_URL = 'https://SEU_PROJETO.supabase.co';

// Chave anônima do Supabase (anon/public key — segura para frontend)
// Encontrada em: Project Settings → API → anon public
var SUPABASE_ANON_KEY = 'SEU_SUPABASE_ANON_KEY_AQUI';
